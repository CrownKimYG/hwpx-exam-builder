# 번호표 출제 비기능 최적화 및 QA — 2026-09-25

대상: `ae7ac6a`의 기존 번호표 출제 화면을 유지한 후속 변경.

## 변경

- 최대 부수 계산에서 반복 시험지 생성 대신 후보 집합을 모은 최대 유량 계산 사용. 번호·단원·묶음·난이도 조건과 문항 중복 제외를 유지한다.
- 계산을 Web Worker로 분리. 새 입력 시 이전 작업을 종료하고, 종료 후 도착한 이전 결과도 무시한다. 계산 실패는 사용자에게 표시하며 출제 버튼을 비활성화한다.
- 여러 시험지를 추가할 때 목록을 매 부마다 다시 그리지 않고 한 번만 갱신한다.
- 목록 렌더링·검증에서 문항 코드 조회용 Map을 재사용한다.
- 진행 중 출제의 중복 클릭을 막는다.

## 성능 측정

로컬 Node 단일 실행, 합성 문항, 번호 10개 모두 전체 후보 허용. 브라우저 전체 처리 시간이나 모든 조건에 대한 보장치는 아니다.

| 문항 수 | 변경 전 | 변경 후 | 계산 결과 |
| --- | --- | --- | --- |
| 1,000 | 1,711ms | 3ms | 양쪽 모두 100부 |
| 10,000 | 미측정 | 11ms | 기존 계산 상한 999부 |

새 계산은 100개의 작은 조건 조합에서 독립적인 전수 탐색 결과와 비교했다. 겹치는 후보의 병목, 사용 이력, 상한, 중복 코드도 검증했다.

## 자동 검증

- JavaScript 172개 통과, Python 4개 통과.
- 프로덕션 빌드 성공, Worker 자산 생성 확인.
- `pnpm audit`: 알려진 취약점 0개. Python 의존성 충돌 없음.
- 손상 ZIP·본문 없는 파일·손상 XML·과대 압축 메타데이터 거부 확인.
- IndexedDB 실패 후 재시도, 기본 템플릿 네트워크 실패 후 재시도 확인.
- 출제 이력 저장 용량 오류 전달 확인.
- SVG의 스크립트·이벤트·외부 참조 차단 확인.
- 합성 HWPX 100/300/600문항 분석: 33/68/120ms. Node+jsdom 측정이며 실제 기기 성능 수치가 아니다.

## 브라우저 검증

로컬 앱의 기존 2개 문제은행(937문항)으로 확인했다. 실제 배포 사이트의 사용자 저장 데이터는 수정하지 않았다.

- 기존 번호표·묶음·입력값·시험지 목록 복원.
- 부수 999 → 2 → 1 연속 변경 후 최신 결과 유지.
- 최대 부수를 초과하는 999부 입력 시 출제 차단.
- 출제 더블클릭 시 15부 → 16부로 한 번만 추가, 자동 저장 완료.
- 시험지 16부의 HWPX 16개 내부 검증 및 ZIP 다운로드 완료.
- 새로고침 후 시험지 16부, 묶음 1개, 최대 29부 계산 결과 및 입력값 복원.
- 1280×640, 390×844에서 문서 가로 넘침 없음. 출제 버튼 영역과 다운로드 바의 경계가 맞아 겹침 없음.
- 콘솔 오류 없음.

## 범위와 한계

- 최대 부수는 기존처럼 999부까지 계산한다.
- 최적화는 기존 번호표 및 은행별 배분의 최대 부수 계산을 대상으로 한다. 혼합 배치는 기존 조건 탐색을 Worker에서 수행하며 탐색 한도와 오류 정책을 유지한다.
- 실패 주입 검사는 가짜 저장소·합성 파일을 사용했다. 실제 사용자 저장소를 채우거나 손상시키지 않았다.
- 의존성 감사 결과는 실행 시점의 알려진 취약점 기준이며 전체 보안 감사를 의미하지 않는다.
